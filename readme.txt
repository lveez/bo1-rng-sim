Some functions for simulating RNG from BO1. (unfinished and abandoned)

The RNG functions are taken from the game, see `bo1-rng-sim/g_rand.cpp` for the assembly. In the game RNG is reseeded with `timeGetTime()` each time a map is loaded.

Assembly is written for MSVC syntax.
